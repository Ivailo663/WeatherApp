// @flow

import { css } from '@emotion/css'

const makeStyles = () => {
  return {
    root: css`
      background: none;
      border: none;
      padding: 0;
      cursor: pointer;
    `,
  }
}

export default makeStyles
